package org.archguard.codedb.gradle

import org.gradle.api.Plugin
import org.gradle.api.Project

const val EXTENSION_NAME = "codedb"

abstract class CodeDBPlugin : Plugin<Project> {
    override fun apply(project: Project) {

    }
}
