/**
 * Chart data is a wrapper around the data that is passed to the chart.
 * Receive JSON data and convert to different formats, for example: Radar, Line, Bar, etc.
 */
export interface ChartData {
  type: string;
}
