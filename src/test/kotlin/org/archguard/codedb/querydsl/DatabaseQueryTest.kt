package org.archguard.codedb.querydsl

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class DatabaseQueryTest {
    @Test
    fun testQuery() {
//        findPublisher = collection.find(and(eq("status", "A"), lt("qty", 30)));
    }
}